# PhotonLink
PhotonLink is an offline peer‑to‑peer file transfer system using optical methods like QR streams, color matrices, and visual frames. It supports chunked transmission, error recovery, compression, encryption, and resumable transfers for diverse file types across devices using only screens and cameras.
